# 🧬 Algorithm History / 算法进化史

这个文件记录 Mini Metro AI Lab 中**所有进入正式实验的算法**，包括最终冠军、阶段性冠军、失败方案和被淘汰方案。

目标不是只留下“最后赢的人”，而是保留完整的工程与研究记忆，避免未来重复走已经验证过的死路，也方便理解冠军策略是怎样一步步演化出来的。

## 记录原则

每个进入 Arena 的算法都必须留下以下信息：

- 算法名称与版本
- 实现提交 / PR
- 核心思路
- 关键参数
- 对战基线
- 固定 Seeds 与模拟预算
- 平均运送量
- 中位数
- 最低 / 最高运送量
- Game Over 率
- 无效动作数量
- 主要优点
- 主要缺点
- 失败案例 / 典型崩局模式
- 最终状态
- 淘汰原因或晋级原因
- 是否值得未来复活

## 状态定义

| 状态 | 含义 |
| --- | --- |
| `baseline` | 当前基准算法 |
| `candidate` | 正在实验或调参 |
| `champion` | 当前默认冠军 |
| `retired` | 曾经有效，但已被更强方案替代 |
| `eliminated` | 经公平对战后淘汰 |
| `archived` | 保留研究价值，但暂不继续投入 |

## 实验纪律

1. **不删除失败算法的历史。** 代码可以移出默认路径，但实验记录必须保留。
2. **不只记录分数。** 还要写清楚它为什么赢、为什么输，以及在哪类局面下会崩。
3. **同一轮比较必须使用相同 Seeds、步长和时间预算。**
4. **任何“新冠军”必须在 Arena 中明确击败当前冠军。**
5. **小样本结果只算候选证据。** 真正晋级前要扩大 Seeds 数量，并观察最差局表现。
6. **被淘汰不等于没价值。** 某些算法可能适合作为搜索启发、局部策略、Value 特征或应急控制器。

---

# 实验记录

## Greedy Planner V1

- **ID**：`greedy-v1`
- **状态**：`baseline`
- **首次进入仓库**：0.1.x
- **核心思路**：
  - 开局按几何距离建立短线路骨架；
  - 新站以最小额外绕行代价插入已有线路；
  - 空线路优先分配机车；
  - 高压力线路优先增加机车与车厢；
  - 通过拓扑冷却避免频繁重画线路。
- **优点**：透明、稳定、计算成本低、容易复现和调试。
- **缺点**：明显短视，只优化局部几何代价和即时拥堵；缺少站型稀缺性、换乘价值、线路均衡和未来客流预测。
- **当前作用**：作为所有后续算法的第一基线。
- **淘汰条件**：新算法在扩大后的固定 Seed 集上稳定提高均值，同时不能显著恶化最差局和 Game Over 率。

## Balanced Greedy V2 · 首次挑战

- **ID**：`balanced-greedy-v2`
- **版本**：`2.0-candidate`
- **状态**：`candidate`
- **实现 PR**：#5
- **对战基线**：`greedy-v1`
- **固定 Seeds**：`42, 314, 2026, 4096, 65537`
- **步长**：100 ms
- **正式预算**：每个算法 × 每个 Seed 最多 15 分钟模拟时间
- **核心思路**：
  - 开局不仅考虑距离，还优先覆盖不同站型；
  - 新站插入评分同时考虑额外绕行、线路长度、线路客流压力和站型多样性；
  - 当已有线路过长且仍有可用线路槽位时，允许另开分流线路；
  - 机车和车厢扩容从固定绝对阈值改为结合网络平均压力的相对判断；
  - 拓扑调整冷却从 700 ms 提高到 900 ms，减少频繁改线。

### 15 分钟固定 Seed 结果

| Seed | Greedy V1 | Balanced V2 | 差值 |
| ---: | ---: | ---: | ---: |
| 42 | 38 | 38 | 0 |
| 314 | 42 | 42 | 0 |
| 2026 | 42 | 42 | 0 |
| 4096 | 35 | 35 | 0 |
| 65537 | 35 | 37 | +2 |

| 指标 | Greedy V1 | Balanced V2 |
| --- | ---: | ---: |
| 平均运送量 | 38.40 | **38.80** |
| 中位数 | 38.00 | 38.00 |
| 最低 | 35 | 35 |
| 最高 | 42 | 42 |
| Game Over 率 | 0% | 0% |
| 无效动作 | 21 | **16** |

### 辅助短局证据

在相同 5 个 Seeds、每局 5 分钟的预赛中：

- Greedy V1 平均 11.80；
- Balanced V2 平均 12.00；
- V2 同样只在少量局面建立微弱优势，但无效动作从 21 次降到 16 次。

### 结论

V2 在首轮 15 分钟实验中平均运送量提高约 **1.0%**，并减少约 **23.8%** 的无效动作；5 个 Seed 中有 4 个完全打平，仅在 Seed `65537` 上以 37:35 拉开差距。

这说明多目标均衡方向**没有造成回退，并表现出更好的动作稳定性**，但证据远不足以宣布新冠军。当前处理：

- 允许进入正式算法库，以 `candidate` 身份供用户选择和观战；
- **Greedy V1 继续保持默认 baseline**；
- V2 不晋级 `champion`；
- 后续扩大 Seed 集、增加更长时域和最坏分位数等指标后再评估；
- 优先检查“新开分流线路”在实际局面中触发不足、收益不明显的问题，并继续调参。

**是否值得继续：是。** V2 已证明它是一个更好的研究起点，但还不是足够强的替代者。

> 历史记录只追加和修正事实，不因算法被淘汰而删除。

## 待进入 Arena

- Beam Search
- Model Predictive Control (MPC)
- Monte Carlo Tree Search (MCTS)
- Search + Value Network
- Policy + Value + Search
- PPO / Recurrent PPO（学习型对照组）
