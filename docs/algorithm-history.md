# 🧬 Algorithm History / 算法进化史

这个文件记录 Mini Metro AI Lab 中**所有进入正式实验的算法**，包括最终冠军、阶段性冠军、失败方案和被淘汰方案。

目标不是只留下“最后赢的人”，而是保留完整的工程与研究记忆，避免未来重复走已经验证过的死路，也方便理解冠军策略是怎样一步步演化出来的。

## 记录原则

每个进入 Arena 的算法都必须留下以下信息：

- 算法名称与版本
- 实现提交 / PR
- 核心思路
- 关键参数
- 对战基线
- 固定 Seeds 与模拟预算
- Simulation Protocol 版本
- Benchmark Scenario 版本
- 平均运送量
- 中位数
- 最低 / 最高运送量
- Game Over 率
- 候车与拥堵健康指标
- 无效动作数量 / 比例
- 主要优点
- 主要缺点
- 失败案例 / 典型崩局模式
- 最终状态
- 淘汰原因或晋级原因
- 是否值得未来复活

## 状态定义

| 状态 | 含义 |
| --- | --- |
| `baseline` | 当前基准算法 |
| `candidate` | 正在实验或调参 |
| `champion` | 当前默认冠军 |
| `retired` | 曾经有效，但已被更强方案替代 |
| `eliminated` | 经公平对战后淘汰 |
| `archived` | 保留研究价值，但暂不继续投入 |

## 实验纪律

1. **不删除失败算法的历史。** 代码可以移出默认路径，但实验记录必须保留。
2. **不只记录分数。** 还要写清楚它为什么赢、为什么输，以及在哪类局面下会崩。
3. **同一轮比较必须使用相同 Seeds、步长、时间预算、场景和 Simulation Protocol。**
4. **任何“新冠军”必须在 Arena 中明确击败当前冠军。**
5. **小样本结果只算候选证据。** 真正晋级前要扩大 Seeds 数量，并观察最差局表现。
6. **被淘汰不等于没价值。** 某些算法可能适合作为搜索启发、局部策略、Value 特征或应急控制器。
7. **协议升级不改写历史。** 旧结果保留原协议标签；需要新口径时重新跑并追加记录。
8. **场景升级也不偷换赛道。** Classic、Stress、Boss 等场景必须显式版本化，禁止把不同压力分布的成绩直接拼成同一排行榜。
9. **资格赛不反向调参。** 一旦候选算法进入冻结 holdout，参数不得根据该 holdout 结果继续调整；后续版本必须使用新的开发集和新的资格赛。

## Simulation Protocol 版本

### Protocol V1 · 0.6.x 及以前

上游引擎在结构化动作被拒绝时不会推进模拟时间，而旧 Arena / CLI Battle 直接进入下一次 planner round。因此出现无效动作的局可能比名义预算少经历少量城市时间，例如 15 分钟实验实际结束于 14.98 分钟。

这不会让旧结果“失效”，但意味着它们只能与同样使用 Protocol V1 的实验直接比较。

### Protocol V2 · 0.7.0 起

拒绝的 planner 动作仍记录为 invalid，但该轮随后以 `noop` 消耗同一个固定 `dt`。除非 Game Over，算法每一轮都承担完全相同的城市时间，因此固定预算不再被无效动作悄悄缩短。

Protocol V2 同时引入时间加权网络健康指标。0.8.0 起又补齐与引擎失败条件一致的 Passenger Pressure：按等待最久的一组乘客相对最大等待时间计算风险，并记录峰值风险、最长等待与高危持续时间。

---

# 实验记录

## Greedy Planner V1

- **ID**：`greedy-v1`
- **状态**：`baseline`
- **首次进入仓库**：0.1.x
- **核心思路**：
  - 开局按几何距离建立短线路骨架；
  - 新站以最小额外绕行代价插入已有线路；
  - 空线路优先分配机车；
  - 高压力线路优先增加机车与车厢；
  - 通过拓扑冷却避免频繁重画线路。
- **优点**：透明、稳定、计算成本低、容易复现和调试。
- **缺点**：明显短视，只优化局部几何代价和即时拥堵；缺少站型稀缺性、换乘价值、线路均衡和未来客流预测。
- **当前作用**：作为所有后续算法的第一基线。
- **淘汰条件**：新算法在扩大后的固定 Seed 集上稳定提高均值，同时不能显著恶化最差局和 Game Over 率。

## Balanced Greedy V2 · 首次挑战

- **ID**：`balanced-greedy-v2`
- **版本**：`2.0-candidate`
- **状态**：`candidate`
- **实现 PR**：#5
- **对战基线**：`greedy-v1`
- **核心思路**：
  - 开局不仅考虑距离，还优先覆盖不同站型；
  - 新站插入评分同时考虑额外绕行、线路长度、线路客流压力和站型多样性；
  - 当已有线路过长且仍有可用线路槽位时，允许另开分流线路；
  - 机车和车厢扩容从固定绝对阈值改为结合网络平均压力的相对判断；
  - 拓扑调整冷却从 700 ms 提高到 900 ms，减少频繁改线。

### Classic V1 · Protocol V1

固定 Seeds `42, 314, 2026, 4096, 65537`，100 ms 步长，15 分钟名义预算：

| Seed | Greedy V1 | Balanced V2 | 差值 |
| ---: | ---: | ---: | ---: |
| 42 | 38 | 38 | 0 |
| 314 | 42 | 42 | 0 |
| 2026 | 42 | 42 | 0 |
| 4096 | 35 | 35 | 0 |
| 65537 | 35 | 37 | +2 |

V2 平均 38.80，V1 平均 38.40。优势约 1%，不足以晋升。

### Classic V1 · Protocol V2

在 PR #8 / 0.7.0 中重新跑相同 Seeds 和预算。所有未 Game Over episode 都精确运行到 15.00 分钟。

| 指标 | Greedy V1 | Balanced V2 |
| --- | ---: | ---: |
| 平均运送量 | 38.40 | **38.80** |
| deliveries / min | 2.56 | **2.59** |
| 时间加权平均候车人数 | 0.37 | **0.35** |
| 平均 Peak Risk | 28.2% | 28.2% |
| 平均最长等待 | 22.2s | **20.9s** |
| Game Over 率 | 0% | 0% |
| invalid action rate | 55.3% | **48.5%** |

Classic 证明 V2 没有明显回退，但也显示这个赛道太温和，无法检验持续扩张下的救火能力。

## Stress V1 · 首次高压挑战

- **Scenario**：`stress-v1`
- **Simulation Protocol**：`V2`
- **固定 Seeds**：`42, 314, 2026, 4096, 65537`
- **步长**：100 ms
- **预算**：15 分钟
- **规则**：固定 Seed 站点池，开局初始站，此后每 45 秒模拟时间开放一个新站直到上限。

### 逐 Seed

| Seed | V1 运送 | V2 运送 | V1 时间 | V2 时间 | V1 Peak Risk | V2 Peak Risk |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 42 | **129** | 94 | **15.00m** | 12.63m | 55% | 100% |
| 314 | 57 | **71** | 9.05m | **10.11m** | 100% | 100% |
| 2026 | **104** | 55 | **13.10m** | 9.35m | 100% | 100% |
| 4096 | **129** | 101 | **15.00m** | 13.15m | 94% | 100% |
| 65537 | 50 | **66** | 8.82m | **10.30m** | 100% | 100% |

### 汇总

| 指标 | Greedy V1 | Balanced V2 |
| --- | ---: | ---: |
| 平均运送量 | **93.80** | 77.40 |
| deliveries / min | **7.42** | 6.89 |
| 时间加权平均候车 | **1.75** | 2.14 |
| 平均 Peak Risk | **89.8%** | 100.0% |
| 平均最长等待 | **48.2s** | 57.1s |
| 平均高危持续 | **18.1s** | 44.2s |
| Game Over 率 | **60%** | 100% |
| invalid action rate | 40.6% | 40.9% |

### Stress V1 结论

1. **Stress V1 成功拉开了算法差异。** V1 平均运送比 V2 高约 21.2%，并且 5 个 Seed 中有两个活满 15 分钟；V2 全部 Game Over。
2. **V2 的 Classic 微弱优势不能外推到高压环境。** 它在低压时更均衡，但持续扩张后平均候车、更长等待和高危持续时间都明显恶化。
3. **Passenger Pressure 与引擎失败机制对齐。** 所有 Game Over episode 的 Peak Risk 均达到 100%；未 Game Over 的 V1 Seed 4096 最高达到 94%，说明它曾进入临界区但成功避免第二个超时乘客同时形成失败条件。
4. **V2 暂不淘汰。** 这次失败给出了明确可修方向：高压时要更积极地集中运力、保留应急资源、减少平均主义式分流，并直接面向等待时间而不是只看线路总压力。

**Balanced V2 当前状态：继续作为 `candidate`，但已确认存在高压失败模式。**

## Balanced Greedy V2.1 · Pressure-aware Rescue

- **ID**：`balanced-greedy-v2-1`
- **版本**：`2.1-candidate`
- **状态**：`candidate`
- **实现 PR**：#10
- **对战基线**：`greedy-v1` 与 `balanced-greedy-v2`
- **Simulation Protocol**：`V2`
- **核心修复**：
  - 发现 V2 的单线扩容条件存在数学死区：只有一条线路时，`busiest_pressure == average_pressure`，所以 `busiest >= average + 1/2` 永远不能成立，V2 常常只给主线第一辆车后就不再扩容；
  - 上游 observation 不公开 passenger `wait_ms`，V2.1 没有穿透读取 `env.mediator`，而是用公开的 `passenger_ids + station_id + time_ms` 重建“连续站台等待 episode”；
  - 当 V2 正常层返回 `noop` 后，rescue layer 使用绝对候车压力与估计等待年龄触发机车 / 车厢投入；
  - 保留 V2 的低压拓扑策略，不把高压救火等同于频繁重画线路。

### 外部 / 上游研究约束

V2.1 不是闭门调权重。上游自身的 semantic heuristic 与实验日志显示：

- 稀疏、合法、简单的动作已经能非常强；
- 当前 pinned 配置下，盲目开第二条线会把有限机车拆散，实验中第二条线显著伤害表现；
- 强 heuristic 的主要形态长期是一条主线吃满有限车队；
- 小规模 held-out 结果可能因生存结果双峰而严重误导，因此必须冻结参数再做更大的资格赛。

这些证据让 V2.1 把重点放在“修复单线运力控制 + 候车年龄感知”，而不是继续扩大多线路复杂度。

### 开发实验 A：900ms rescue cooldown

仍使用旧的 5 个开发 Seeds `42, 314, 2026, 4096, 65537`。

- V2：Stress 平均 77.40，Game Over 100%。
- V2.1 初版：Stress 平均 **91.40**，Game Over 降到 **60%**。
- 但 Seed `2026` 只有 65，而 V1 是 104。

这证明“单线无法继续扩容”确实是 V2 的主要洞之一，但 900ms rescue cooldown 是没有实验依据的人为节流。

### 开发实验 B：冻结前最终参数

只移除上述 900ms rescue cooldown，不再改其他参数。

| 指标 | Greedy V1 | Balanced V2 | Balanced V2.1 |
| --- | ---: | ---: | ---: |
| Classic 平均运送 | 38.40 | 38.80 | **38.80** |
| Classic invalid rate | 55.3% | 48.5% | **34.2%** |
| Stress 平均运送 | 93.80 | 77.40 | **106.00** |
| Stress D/min | 7.42 | 6.89 | **7.88** |
| Stress 平均候车 | **1.75** | 2.14 | 1.98 |
| Stress Peak Risk | **89.8%** | 100.0% | 90.6% |
| Stress 高危持续 | **18.1s** | 44.2s | 36.3s |
| Stress Game Over | 60% | 100% | 60% |

开发集上 V2.1 比 V1 高约 13%，Seed `2026` 从 65 恢复到 103，表明“不要人为拖慢连续运力部署”的判断成立。但这个 5-Seed 集已经参与了两轮诊断，所以**不能用它宣布冠军**。

### 冻结 20-Seed holdout qualification

V2.1 参数冻结后，使用 `random.Random(20260905)` 一次性生成 20 个此前完全未参与调参的 Seeds。生成后先写入 CI，再运行结果；此后这些 Seeds 不允许反向用于 V2.1 调参。

#### Classic V1 holdout

| 指标 | Greedy V1 | Balanced V2.1 |
| --- | ---: | ---: |
| 平均运送 | 39.50 | **39.80** |
| 平均候车 | 0.32 | **0.30** |
| Peak Risk | 25.1% | **23.6%** |
| 平均最长等待 | 17.9s | **17.0s** |
| Game Over | 0% | 0% |
| invalid rate | 33.3% | **23.3%** |

Classic 下 V2.1 没有出现泛化回退，并继续表现出更少的无效动作。

#### Stress V1 holdout

| 指标 | Greedy V1 | Balanced V2.1 |
| --- | ---: | ---: |
| 平均运送 | **107.30** | 103.75 |
| D/min | **7.90** | 7.77 |
| 平均候车 | 1.95 | 1.95 |
| Peak Risk | **94.2%** | 97.2% |
| 平均最长等待 | **48.8s** | 49.3s |
| 高危持续 | **28.0s** | 33.0s |
| Game Over | 65% | 65% |
| invalid rate | 39.6% | **32.2%** |

逐 Seed paired 结果为 **V2.1 8 胜 / 11 负 / 1 平**；平均 deliveries 差值 `-3.55`，中位差值 `-1`。因此开发集上 106.0 的领先**没有在冻结 holdout 中复现**。

### V2.1 结论

1. **V2.1 是一个真实修复，不是伪进步。** 它修复了 V2 的单线运力死区，把开发集 Stress 从 77.4 拉回 106.0，并在 Classic / holdout 都显著降低 invalid rate。
2. **V2.1 不是新冠军。** 冻结 Stress holdout 中 V1 仍以 107.30 对 103.75 领先，且风险 / 高危时间略优。
3. **默认算法继续保持 Greedy V1。** V2.1 以 `candidate` 身份进入算法库，供后续搜索、路线优化和观战比较使用。
4. **禁止继续针对这 20 个 holdout Seeds 调 V2.1。** 下一版本若继续开发，必须使用新的 development seeds，并预先冻结新的 qualification set。
5. **下一高价值方向不是继续拧 rescue 阈值。** 上游实验显示 route re-layout / 2-opt 类线路重排存在明显结构性 headroom，适合进入下一代候选。

> V2.1 的价值在于把“为什么 V2 会死”变成了可复现工程事实，也建立了开发集 / 冻结资格赛分离的算法实验纪律。

> 历史记录只追加和修正事实，不因算法被淘汰而删除。

## 待进入 Arena

- Route Re-layout / 2-opt candidate
- Boss Seeds
- Beam Search
- Model Predictive Control (MPC)
- Monte Carlo Tree Search (MCTS)
- Search + Value Network
- Policy + Value + Search
- PPO / Recurrent PPO（学习型对照组）
